services:
  hoeng-bot:
    build: .
    container_name: hoeng-bot
    restart: always
    network_mode: host
    environment:
      - TZ=Asia/Seoul
      - BOT_TOKEN=${BOT_TOKEN}
    volumes:
      - ./config.js:/bot/config.js
      - ./logs:/bot/logs