services:
  hoeng-bot:
    image: ghcr.io/hoehoetvhss/hoeng-bot:latest
    container_name: hoeng-bot
    restart: always
    network_mode: host
    environment:
      - TZ=Asia/Seoul
      - BOT_TOKEN=${BOT_TOKEN}
    volumes:
      - ./config.js:/bot/config.js
      - ./data:/bot/data
      - ./logs:/bot/logs
      - ./views/dashboard.html:/bot/views/dashboard.html
    labels:
      - "com.centurylinklabs.watchtower.enable=true"
